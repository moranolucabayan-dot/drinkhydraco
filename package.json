{
  "name": "hydraco",
  "version": "1.0.0",
  "description": "Hydraco Restore — Daily Electrolyte Recovery Mix",
  "private": true,
  "scripts": {
    "build": "echo 'Static HTML - no build needed'",
    "dev": "python3 -m http.server 3000 || python -m http.server 3000"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1"
  }
}
